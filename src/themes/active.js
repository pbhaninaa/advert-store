/**
 * ACTIVE THEME — change which preset is loaded here.
 *
 * Options:
 *   import theme from './presets/clothing.js'   ← clothing reseller (default)
 *   import theme from './presets/custom.js'     ← paste your own colors
 *
 * Or paste colors directly into the `colors` object below.
 */
import theme from './presets/clothing.js'

export default theme
